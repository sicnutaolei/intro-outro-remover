# Rust 构建产物
/target
**/*.rs.bk

# 测试与运行时的临时文件
*.pdb
*.ilk

# needle 的工作产物（正常情况下跑完会自动清理，这里是防止手动跑 needle 后误提交）
# 两个哈希文件名都要列：官方 v0.1.5 写 .needle.bin，main 分支写 .needle.dat
*.needle.bin
*.needle.dat
*.needle.skip.json

# 切割用的临时目录（正常情况跑完即删，留一份保险）
.intro-outro-tmp-*/

# 编辑器
.vscode/
.idea/
*.swp
.DS_Store
